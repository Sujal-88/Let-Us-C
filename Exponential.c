#include <stdio.h>
#include<math.h>
// To perform exponential operations you have to use the "pow" function.
int main(){
    float a=2; 
    int b=16;
    a=sqrt(a);
    b=sqrt(b);
    //The first num is to which you have to give power and second one is to which you wanted the power.
    printf("the Answer is-->%f", a);
    printf("the Answer is-->%d", b);
    return 0;
}